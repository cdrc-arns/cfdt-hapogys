<% @articles.each do |article| %>
  <div class="card-wrapper">
    <div class="article-title">
      <h2><%= article.title %></h2>
    </div>
    <div class="article-description">
      <%= article.description %>
    </div>
  </div>
<% end %>
